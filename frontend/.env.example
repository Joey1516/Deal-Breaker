VITE_API_BASE=http://localhost:8787

# Same OAuth Client ID configured as GOOGLE_CLIENT_ID on the backend. This value is
# public (it's meant to be embedded in client JS) — safe to bake into the build.
# Leave unset to hide the "Continue with Google" button entirely.
VITE_GOOGLE_CLIENT_ID=
